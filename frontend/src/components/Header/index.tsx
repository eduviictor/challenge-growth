import React from 'react';
import Logo from '../Logo';

import { HeaderWrapper } from './styles';

function Header() {
  return (
    <HeaderWrapper>
      <Logo />
    </HeaderWrapper>
  );
}

export default Header;
